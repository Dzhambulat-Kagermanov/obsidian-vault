|   |   |   |           vCenter logs: `/var/log/vmware/vpxd/vpxd.log`, `/var/log/vmware/sso/`.
|   |   |   |           ESXi logs: `/var/log/vmkernel.log`, `/var/log/hostd.log`, `/var/log/vobd.log`.
|   |   |   |           DCUI (Direct Console UI): F2 (customize), F12 (shutdown/reboot), Troubleshooting mode.
|   |   |   |           SSH to ESXi: enable via DCUI or Host → Actions → Services → Enable SSH.
|   |   |   |           Common issues: VM not power on (insufficient resources), vMotion fails (CPU incompatibility), datastore full.