|   |   |   |           Расположение логов: `/var/log/<service>/` (nova, neutron, cinder, glance, keystone).
|   |   |   |           Структура логов: <service>-api.log, <service>-conductor.log, <service>-scheduler.log.
|   |   |   |           Debug logging: `debug = True` в `/<service>/<service>.conf`.
|   |   |   |           Correlation IDs: отслеживание запроса через все сервисы.
|   |   |   |           API debugging: `curl -v` с токеном, анализ ответов.
|   |   |   |           Oslo.messaging: отладка RabbitMQ очередей, dead letter queues.