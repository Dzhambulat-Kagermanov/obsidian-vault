|   |   |   |           Логи: `/var/log/syslog`, `/var/log/pveproxy/access.log`, `journalctl -u pve-*`.
|   |   |   |           VM не стартует: `qm config VMID`, `qm showcmd VMID`, проверка storage.
|   |   |   |           Network issues: `tcpdump`, `bridge link show`, `ovs-vsctl show`.
|   |   |   |           Storage problems: `pvesm status`, `zfs status`, `ceph -s`.
|   |   |   |           Cluster split-brain: quorum loss, manual quorum (`pvecm expected 1`).