|   |           Зачем нужна изоляция: предотвращение конфликтов с хост-системой.
|   |           Создание пользователя `lfs`: `useradd -s /bin/bash -m lfs`.
|   |           Настройка окружения: `.bash_profile`, `.bashrc` для пользователя `lfs`.
|   |           Переменные: `LC_ALL`, `LFS`, `LFS_TGT` (target triplet).
|   |           Проверка: `env | grep LFS`.