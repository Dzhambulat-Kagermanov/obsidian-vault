**Цель:** Достижение 100/100 в Lighthouse и минимального Time to First Byte (TTFB). **Что записать:**

- **`next/image`:** Автоматическая оптимизация, форматы (AVIF/WebP), использование пропса `sizes` для корректной загрузки изображений под разные viewport (критично для LCP).
- **`next/font`:** Оптимизация шрифтов (Google Fonts или локальные). Автоматический self-hosting, устранение Layout Shift (CLS) и блокировки рендеринга.
- **`next/script`:** Стратегии загрузки сторонних скриптов (`beforeInteractive`, `afterInteractive`, `lazyOnload`, `worker` для выноса в Web Worker).
- **Динамический импорт:** `next/dynamic` с `{ ssr: false }` для тяжелых клиентских библиотек (например, Rich Text Editors или сложные графики), которые не нужны для первоначального рендера.
- **Анализ бандла:** Использование `@next/bundle-analyzer` для поиска тяжелых зависимостей.