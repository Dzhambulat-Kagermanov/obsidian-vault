// Обычный setState → высокий приоритет
// startTransition(() => setState(...)) → низкий приоритет
// React может прервать низкоприоритетную работу при срочном обновлении

**Важно:** startTransition не делает синхронный код асинхронным. Только меняет приоритет обновлений состояния.