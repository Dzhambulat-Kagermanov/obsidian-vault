// ✅ ХОРОШО
key={item.id}

// ❌ ПЛОХО
key={index}           // при перестановке — пересоздание
key={Math.random()}   // всегда разный → всегда пересоздание
key={item.name}       // может быть неуникальным

**Почему index плох:** React не может отличить перемещение от удаления/создания