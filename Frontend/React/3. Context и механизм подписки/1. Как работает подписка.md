// Внутренний механизм (упрощенно)
const contextSubscribers = new Map();

// Компонент вызывает useContext → добавляется в subscribers
// Provider меняет значение → React проходит по subscribers
// Перерендериваются ТОЛЬКО подписанные компоненты

**Ключевое:** React не перерендеривает всё дерево от App — только подписанные компоненты!