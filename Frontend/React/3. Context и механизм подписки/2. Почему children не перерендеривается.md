```tsx
// Передается из родителя → тот же объект → нет перерендера
<Provider>
  {children}  {/* ссылка не меняется */}
</Provider>

// Создается внутри → новый объект → перерендер
<Provider>
  <Child />  {/* создается заново */}
</Provider>
```