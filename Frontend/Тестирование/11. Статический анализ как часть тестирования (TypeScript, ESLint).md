**Цель:** Сдвинуть обнаружение ошибок максимально влево (до запуска кода и тестов). **Что записать:**

- **TypeScript как тест:** Почему `strict: true` в `tsconfig.json` заменяет сотни unit-тестов на проверку типов. Примеры utility types (`Partial`, `Record`, `ReturnType`) для тестов.
- **Специфичные ESLint-плагины:**
    - `eslint-plugin-testing-library` (следит за правильным использованием RTL).
    - `eslint-plugin-jest` / `eslint-plugin-vitest` (предупреждает о забытых `await` или бесполезных assert).
- **Pre-commit хуки:** Настройка Husky + lint-staged для запуска линтера и типизации только на измененных файлах.