**Цель:** Избежать ошибок "window is not defined" и рассинхронизации гидратации. **Что записать:**

- Динамический импорт GSAP: `const gsap = typeof window !== "undefined" ? require("gsap") : null`.
- Использование `useEffect` (Next.js) или `onMounted` (Nuxt) как единственного места для инициализации DOM-анимаций.
- Отключение анимаций на сервере или установка начальных стилей через CSS, чтобы избежать "мигания" при гидратации.