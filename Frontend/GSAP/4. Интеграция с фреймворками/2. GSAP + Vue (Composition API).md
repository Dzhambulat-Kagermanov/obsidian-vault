**Цель:** Адаптировать знания под реактивность Vue. **Что записать:**

- Использование хуков `onMounted` для инициализации и `onBeforeUnmount` для `kill()`.
- Работа с `ref` и `template refs` (`ref="myElement"`).
- Реактивность: как перезапускать анимацию при изменении пропсов (с помощью `watch` и `revert()`).