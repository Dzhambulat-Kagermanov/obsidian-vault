**Цель:** Сделать сайт удобным для всех пользователей и соответствовать стандартам. **Что записать:**

- Медиа-запрос `@media (prefers-reduced-motion: reduce)`.
- **gsap.matchMedia():** Как элегантно включать/отключать или упрощать анимации в зависимости от этого запроса или ширины экрана (mobile vs desktop).
- Пример: если пользователь выбрал уменьшенное движение, делать `duration: 0` или анимировать только `opacity`, игнорируя `x`/`y`/`scale`.