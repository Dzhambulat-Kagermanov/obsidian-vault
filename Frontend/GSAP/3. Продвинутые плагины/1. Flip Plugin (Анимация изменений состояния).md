**Цель:** Освоить самую мощную технику для анимации перестроения макета (Layout). **Что записать:**

- Что такое FLIP (First, Last, Invert, Play).
- Синтаксис: `const state = Flip.getState(".items")`, изменение DOM, `Flip.from(state, { duration: 1, ease: "power1.inOut" })`.
- Практические кейсы: фильтрация списка, раскрытие карточки на весь экран, смена раскладки (grid ↔ list).