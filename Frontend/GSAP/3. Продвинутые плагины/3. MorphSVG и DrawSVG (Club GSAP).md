**Цель:** Правильно использовать GSAP в экосистеме React, избегая антипаттернов. **Что записать:**

- Почему старый подход с `useEffect` и созданием переменных вне его — это плохая практика.
- Использование официального хука `@gsap/react` (`useGSAP`).
- Как правильно получать ссылки на элементы: `useRef` + `gsap.utils.selector()` или `gsap.context()`.
- Пример компонента с анимацией появления и корректной очисткой при `unmount`.