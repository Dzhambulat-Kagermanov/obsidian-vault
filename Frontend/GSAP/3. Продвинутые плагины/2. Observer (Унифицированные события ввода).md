**Цель:** Создавать кастомные интерактивные элементы, реагирующие на колесо мыши, тач и свайпы одинаково. **Что записать:**

- Конфигурация `Observer`: `type: "wheel,touch,pointer"`, `onUp`, `onDown`, `onLeft`, `onRight`.
- Использование для создания кастомных горизонтальных слайдеров или колес прокрутки (как на сайте Apple).