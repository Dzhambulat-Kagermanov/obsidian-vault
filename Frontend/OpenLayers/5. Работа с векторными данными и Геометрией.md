**Цель:** Манипулирование данными на карте: фильтрация, поиск, расчеты. **Что записать:**

- Структура: `Feature` (содержит `Geometry` и `Properties`) vs `Geometry` (Point, LineString, Polygon, MultiPolygon).
- Пространственные запросы: как найти все точки внутри видимой области карты (`map.getView().calculateExtent()` + `intersectsExtent`).
- Чтение и запись форматов: `ol/format/GeoJSON`, `ol/format/WKT`. Как распарсить пришедший с бэкенда GeoJSON и добавить на карту.
- Вычисление площади полигона или длины линии (`ol/sphere/getLength`, `getArea`).