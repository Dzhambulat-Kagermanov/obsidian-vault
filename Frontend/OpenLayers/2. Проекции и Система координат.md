**Цель:** Решить главную боль GIS во фронтенде — когда точки отображаются не там или карта "улетает" в океан. **Что записать:**

- Базовые проекции: `EPSG:3857` (Web Mercator, дефолт в OL, используется для тайлов) vs `EPSG:4326` (WGS84, классические широта/долгота из GPS/GeoJSON).
- Утилиты трансформации: `fromLonLat`, `toLonLat`, `ol.proj.transform`.
- Как зарегистрировать кастомную проекцию (например, локальную систему координат здания или `EPSG:4284` / `EPSG:32637` для РФ) с помощью `proj4js` и `register(proj4)`.
- Отображение координат курсора в нужной проекции (`MousePosition` control).